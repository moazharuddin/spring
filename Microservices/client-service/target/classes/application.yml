# Spring properties
spring:
  application:
    name: client-service

# Discovery Server Access
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:9099
  instance: localhost

# HTTP Server
server:
  port: 8031   # HTTP (Tomcat) port